name: Build & Publish Docker Images

on:
  push:
    tags:
      - "v*"
  # Smoke-test the image on PRs that can break the Docker runtime environment
  # (the build job below only runs on tags / manual dispatch)
  pull_request:
    paths:
      - "docker/**"
      - "docker-compose.yml"
      - "apps/background-removal/**"
      - "packages/db/**"
      - "apps/web/next.config.*"
      - "apps/web/env.ts"
      - "packages/auth/**"
      - "apps/web/instrumentation.ts"
      - "apps/web/instrumentation-client.ts"
      - "apps/web/proxy.ts"
      - "apps/web/components/shared/posthog-bootstrap.tsx"
      - "apps/web/components/shared/public-env-provider.tsx"
      - "apps/web/lib/posthog-client.ts"
      - "apps/web/lib/validators/validator.public-env.ts"
      - "apps/web/lib/util.security-headers.ts"
      - "apps/web/app/api/health/**"
      - "apps/web/app/api/standalone/**"
      - "pnpm-lock.yaml"
      - ".github/workflows/docker.yml"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build (e.g., v1.0.0)"
        required: false
        default: "latest"

env:
  REGISTRY_GITHUB: ghcr.io
  REGISTRY_DOCKER: docker.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # Boots the production image against an empty MySQL and verifies the app
  # actually sets up its schema and reports healthy. Catches the class of bug
  # where something works in dev but is missing from the pruned standalone
  # image (see issue #28).
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build image for smoke test
        uses: docker/build-push-action@v7
        with:
          context: .
          file: docker/Dockerfile.web
          target: app
          platforms: linux/amd64
          load: true
          tags: louez-web:smoke
          build-args: |
            NEXT_PUBLIC_APP_VERSION=${{ github.sha }}
          cache-from: type=gha,scope=web
          cache-to: type=gha,mode=max,scope=web

      - name: Build background-removal image for smoke test
        uses: docker/build-push-action@v7
        with:
          context: .
          file: apps/background-removal/Dockerfile
          platforms: linux/amd64
          load: true
          tags: louez-background-removal:smoke
          cache-from: type=gha,scope=background-removal
          cache-to: type=gha,mode=max,scope=background-removal

      - name: Verify the web image contains no runtime marker
        run: |
          if docker run --rm --entrypoint sh louez-web:smoke -c \
            'grep -R -F "runtime-public-config-smoke" /app/apps/web/.next/static'; then
            echo "❌ Runtime configuration leaked into the immutable client bundles"
            exit 1
          fi
          echo "✅ Client bundles remain environment-agnostic"

      - name: Start MySQL
        run: |
          docker network create louez-net
          docker run -d --name louez-db --network louez-net \
            -e MYSQL_ROOT_PASSWORD=smoketest \
            -e MYSQL_DATABASE=louez \
            mysql:8.4
          echo "Waiting for MySQL to accept connections..."
          for _ in $(seq 1 60); do
            if docker exec louez-db mysqladmin ping -h 127.0.0.1 -uroot -psmoketest --silent 2>/dev/null; then
              echo "MySQL is ready"
              exit 0
            fi
            sleep 2
          done
          echo "MySQL never became ready"
          docker logs louez-db
          exit 1

      - name: Start and verify background-removal worker
        run: |
          docker run -d --name louez-background-removal \
            --network louez-net -p 7000:7000 \
            -e BACKGROUND_REMOVAL_API_TOKEN=smoke-test-background-removal-token \
            louez-background-removal:smoke
          echo "Waiting for the background-removal model to load..."
          for i in $(seq 1 90); do
            if curl -fsS http://localhost:7000/health >/dev/null; then
              break
            fi
            if [ "$i" = "90" ]; then
              echo "Background-removal worker never became healthy"
              docker logs louez-background-removal
              exit 1
            fi
            sleep 2
          done
          test "$(curl -sS -o /dev/null -w '%{http_code}' \
            -F "file=@apps/web/public/favicon-32x32.png;type=image/png" \
            -F "model=u2netp" \
            http://localhost:7000/api/remove)" = "401"
          curl -fsS \
            -H "Authorization: Bearer smoke-test-background-removal-token" \
            -F "file=@apps/web/public/favicon-32x32.png;type=image/png" \
            -F "model=u2netp" \
            http://localhost:7000/api/remove \
            --output /tmp/background-removed.png
          test -s /tmp/background-removed.png
          test "$(file --brief --mime-type /tmp/background-removed.png)" = "image/png"
          echo "✅ Background removal worker OK"

      - name: Boot app against empty database
        run: |
          docker run -d --name louez-app --network louez-net -p 3000:3000 \
            -e DATABASE_URL=mysql://root:smoketest@louez-db:3306/louez \
            -e AUTH_SECRET=smoke-test-secret-not-a-real-secret \
            -e BETTER_AUTH_SECRET=smoke-test-secret-not-a-real-secret \
            -e S3_ENDPOINT=http://smoke-test.invalid:9000 \
            -e S3_REGION=smoke \
            -e S3_BUCKET=smoke-test \
            -e S3_ACCESS_KEY_ID=smoke-test \
            -e S3_SECRET_ACCESS_KEY=smoke-test \
            -e S3_PUBLIC_URL=/media \
            -e NEXT_PUBLIC_APP_URL=http://localhost:3000 \
            -e NEXT_PUBLIC_APP_DOMAIN=localhost:3000 \
            -e NEXT_PUBLIC_GLEAP_API_KEY=runtime-public-config-smoke-standalone \
            -e NEXT_PUBLIC_FROMHELLO_API_URL=https://runtime-public-config-smoke-standalone.example.invalid \
            -e NEXT_PUBLIC_OPENREPLAY_INGEST_POINT=https://runtime-public-config-smoke-replay-standalone.example.invalid/ingest \
            -e AI_IMAGE_BACKGROUND_REMOVAL_URL=http://louez-background-removal:7000 \
            -e AI_IMAGE_BACKGROUND_REMOVAL_TOKEN=smoke-test-background-removal-token \
            louez-web:smoke

      - name: Wait for app to report healthy
        run: |
          for _ in $(seq 1 60); do
            if [ "$(docker inspect -f '{{.State.Running}}' louez-app)" != "true" ]; then
              echo "App container exited prematurely"
              docker logs louez-app
              exit 1
            fi
            status=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/api/health || true)
            if [ "$status" = "200" ]; then
              echo "App is healthy"
              exit 0
            fi
            sleep 2
          done
          echo "Health endpoint never returned 200 (last status: $status)"
          docker logs louez-app
          exit 1

      - name: Verify core tables were created
        run: |
          tables=$(docker exec louez-db mysql -uroot -psmoketest -N -e "SHOW TABLES" louez)
          echo "Tables in database:"
          echo "$tables"
          for t in users accounts sessions stores products reservations; do
            if ! echo "$tables" | grep -qx "$t"; then
              echo "❌ Missing core table: $t"
              docker logs louez-app
              exit 1
            fi
          done
          echo "✅ All core tables present"

      - name: Verify standalone client runtime configuration
        run: |
          html=$(curl -fsSL http://localhost:3000/login)
          if ! echo "$html" | grep -Fq "runtime-public-config-smoke-standalone"; then
            echo "❌ Runtime public value is absent from the React bootstrap payload"
            exit 1
          fi
          if ! echo "$html" | grep -Fq "${{ github.sha }}"; then
            echo "❌ Build version is absent from the React bootstrap payload"
            exit 1
          fi
          headers=$(curl -fsS -D - -o /dev/null http://localhost:3000/login | tr -d '\r')
          if ! echo "$headers" | grep -Fi "content-security-policy:" | \
            grep -Fq "https://runtime-public-config-smoke-standalone.example.invalid"; then
            echo "❌ Runtime fromHello origin is absent from the CSP"
            exit 1
          fi
          if ! echo "$headers" | grep -Fi "content-security-policy:" | \
            grep -Fq "https://runtime-public-config-smoke-replay-standalone.example.invalid"; then
            echo "❌ Runtime OpenReplay origin is absent from the CSP"
            exit 1
          fi
          echo "✅ Standalone browser config comes from the container runtime"

      # The container above runs in standalone mode (LOUEZ_MODE unset — the
      # default): a fresh install must 404 the instance-store resolver and
      # walk visitors from the root to the login flow.
      - name: Verify standalone-mode first-run routing
        run: |
          status=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/api/standalone/store)
          if [ "$status" != "404" ]; then
            echo "❌ Expected 404 from /api/standalone/store on an empty instance, got $status"
            exit 1
          fi
          root=$(curl -s -o /dev/null -w '%{http_code}' -L http://localhost:3000/)
          if [ "$root" != "200" ]; then
            echo "❌ Expected 200 after redirects on /, got $root"
            exit 1
          fi
          if ! curl -s -L http://localhost:3000/ | grep -qi "login"; then
            echo "❌ Root did not land on the login flow"
            exit 1
          fi
          echo "✅ Standalone first-run routing OK"

      - name: Boot platform mode against the same database
        run: |
          docker run -d --name louez-app-platform --network louez-net -p 3001:3000 \
            -e LOUEZ_MODE=platform \
            -e DATABASE_URL=mysql://root:smoketest@louez-db:3306/louez \
            -e AUTH_SECRET=smoke-test-secret-not-a-real-secret \
            -e BETTER_AUTH_SECRET=smoke-test-secret-not-a-real-secret \
            -e S3_ENDPOINT=http://smoke-test.invalid:9000 \
            -e S3_REGION=smoke \
            -e S3_BUCKET=smoke-test \
            -e S3_ACCESS_KEY_ID=smoke-test \
            -e S3_SECRET_ACCESS_KEY=smoke-test \
            -e S3_PUBLIC_URL=/media \
            -e NEXT_PUBLIC_APP_URL=http://localhost:3001 \
            -e NEXT_PUBLIC_APP_DOMAIN=localhost:3001 \
            -e NEXT_PUBLIC_GLEAP_API_KEY=runtime-public-config-smoke-platform \
            -e NEXT_PUBLIC_FROMHELLO_API_URL=https://runtime-public-config-smoke-platform.example.invalid \
            louez-web:smoke
          status=""
          for _ in $(seq 1 60); do
            status=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3001/api/health || true)
            if [ "$status" = "200" ]; then break; fi
            sleep 2
          done
          if [ "$status" != "200" ]; then
            echo "❌ Platform-mode container never became healthy (last status: $status)"
            docker logs louez-app-platform
            exit 1
          fi
          resolver=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3001/api/standalone/store)
          if [ "$resolver" != "404" ]; then
            echo "❌ Platform mode must not expose the instance-store resolver (got $resolver)"
            exit 1
          fi
          html=$(curl -fsSL http://localhost:3001/login)
          if ! echo "$html" | grep -Fq "runtime-public-config-smoke-platform"; then
            echo "❌ Second runtime value is absent from the React bootstrap payload"
            exit 1
          fi
          if echo "$html" | grep -Fq "runtime-public-config-smoke-standalone"; then
            echo "❌ Runtime configuration leaked between containers"
            exit 1
          fi
          if ! echo "$html" | grep -Fq "${{ github.sha }}"; then
            echo "❌ Build version changed between runtime configurations"
            exit 1
          fi
          headers=$(curl -fsS -D - -o /dev/null http://localhost:3001/login | tr -d '\r')
          if ! echo "$headers" | grep -Fi "content-security-policy:" | \
            grep -Fq "https://runtime-public-config-smoke-platform.example.invalid"; then
            echo "❌ Second runtime origin is absent from the CSP"
            exit 1
          fi
          echo "✅ One image boots with two isolated runtime browser configurations"

      - name: Dump app logs
        if: always()
        run: |
          docker logs louez-app 2>&1 || true
          docker logs louez-app-platform 2>&1 || true
          docker logs louez-background-removal 2>&1 || true

  build:
    runs-on: ubuntu-latest
    needs: smoke-test
    if: startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch'
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY_GITHUB }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ${{ env.REGISTRY_GITHUB }}/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest,enable={{is_default_branch}}
            type=raw,value=${{ inputs.tag }},enable=${{ github.event_name == 'workflow_dispatch' }}

      - name: Extract background-removal metadata
        id: background-meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ${{ env.REGISTRY_GITHUB }}/${{ github.repository }}-background-removal
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest,enable={{is_default_branch}}
            type=raw,value=${{ inputs.tag }},enable=${{ github.event_name == 'workflow_dispatch' }}

      - name: Build and push web image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: docker/Dockerfile.web
          target: app
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            NEXT_PUBLIC_APP_VERSION=${{ github.sha }}
          cache-from: type=gha,scope=web
          cache-to: type=gha,mode=max,scope=web

      - name: Build and push background-removal image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: apps/background-removal/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.background-meta.outputs.tags }}
          labels: ${{ steps.background-meta.outputs.labels }}
          cache-from: type=gha,scope=background-removal
          cache-to: type=gha,mode=max,scope=background-removal
